{
  "name": "loopback-seed",
  "version": "1.0.0",
  "description": "component for loopback app, use to seed your database(s)",
  "main": "./dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "faker": "^3.1.0",
    "fs": "0.0.1-security",
    "path": "^0.12.7",
    "require": "^2.4.20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rflipo-dev/loopback-seed.git"
  },
  "keywords": [
    "loopback",
    "seed",
    "database"
  ],
  "author": "rflipo-dev",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-preset-es2015": "^6.14.0"
  }
}
